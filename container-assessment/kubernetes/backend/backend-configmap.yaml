apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-env
  namespace: muchtodo
data:
  .env: |
    PORT=3000
    MONGO_URI=mongodb://admin:password@mongodb:27017/muchtodo?authSource=admin
    DB_NAME=muchtodo
