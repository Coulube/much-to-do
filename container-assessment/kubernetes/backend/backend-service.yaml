apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: muchtodo
spec:
  type: NodePort
  selector:
    app: muchtodo-backend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30080
